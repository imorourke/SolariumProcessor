[workspace]

members = [
    "cbuoy",
    "cbc",
    "jib-asm",
    "jib",
    "visual-jib",
    "cbfs",
    "cbfs-bin",
]
default-members = ["visual-jib"]
resolver = "2"

[profile.release]
lto = true
strip = true
