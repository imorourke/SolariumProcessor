[workspace]

members = [
    "cbuoy",
    "cbuoy-cbc",
    "cbfs/compat",
    "cbfs-lib",
    "cbfs-util",
    "jib-asm",
    "jib",
    "visual-jib",
]
default-members = ["visual-jib"]
resolver = "2"

[profile.release]
lto = true
strip = true
