[workspace]

members = [
    "cbuoy",
    "cbc",
    "jib-asm",
    "jib",
    "visual-jib-gtk",
    "visual-jib-egui",
]
default-members = ["visual-jib-egui"]
resolver = "2"

[profile.release]
lto = true
strip = true
