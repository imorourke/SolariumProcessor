[workspace]

members = [
    "cbuoy",
    "cbc",
    "jib-asm",
    "jib",
    "visual-jib-gtk",
    "visual-jib-egui",
    "visual-jib-lib",
]
default-members = ["visual-jib-egui"]
resolver = "2"

[profile.release]
lto = true
strip = true
